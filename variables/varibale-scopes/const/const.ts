// Const ensures that once a variable is assigbned a value, it can not be changed.
// However Typescript does not stricktly enforece const like in other programming languages
// because ES6 supported in by all WEB BROWSERS.

const pi = 3.14;
pi = 45;

console.log(pi);