const product = function (x:Number, y:number):Number{
    return x * y;
}

// pruduct variable can not be delcared againg since it 
// has already been assigned to const prefix.
product = function (x: Number, y: number): number{
 retuen x * y;   
}
console.log(product);